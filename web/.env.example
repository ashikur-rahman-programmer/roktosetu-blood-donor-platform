# The public URL of your deployed backend (server/), no trailing slash.
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com

# Must match DONATION_GAP_DAYS on the server so the UI's eligibility
# countdown matches what the backend actually enforces.
NEXT_PUBLIC_DONATION_GAP_DAYS=90
